(*	GAMEMECHANICS.ML
 *	
 *	Magic engine.
 *	Game physics functions are called here to iterate through the game.
 *)

open Gametypes
open Gamephysics

let iterate_player (pos, vel, modif) context =
	()	(* returns new player *)

let iterate_game context =
	()	(* forall players *)