(*	PHYSICS.ML
 *	
 *	Where magic is loaded and stored.
 *	Levels are loaded and stored here (Will be used for the editor).
 *)

(*	Object loading *)
let loadObject str =
	()	(*	TODO	*)
(*	Level loading *)
let loadLevel file =
	()	(*	TODO	*)